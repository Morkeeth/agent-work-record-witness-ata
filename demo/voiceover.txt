@voice am_michael
@lang b
@pause 300

Run your agents. Check the math.

Every company running AI agents can see two things. How many seats they bought, and how many tokens those agents burned. What they cannot see is whether the work the agents said they did, they actually did.

An agent writes a sentence. Committed as deadbee. Nothing checks that sentence against the repository, and at fleet scale, that sentence is what auto-merge reads.

So we check it. The gate takes the claim, turns it into a probe, and runs the probe against the object. Git cat file. Does that commit exist. It does not, so the pull request is blocked.

A check that only ever says no is not a check. Here is an honest report. Real commit, real file, and it passes.

And here is the one it refuses to guess at. The commit checks out. All fourteen tests pass does not, because verifying that would mean running a command lifted from agent prose. It is held, not guessed.

The backend is live on Google Cloud Run. This is the deployed service, its URL, and its revision. Firestore holds the record. Gemini three point five, through the Agent Development Kit, explains a verdict. It never overrules one.

And eligibility is not an import. This script calls all three services rather than importing them. With credentials it prints three of three and exits zero. Cold, with no credentials, it prints one of three and exits one, deliberately, because a class name proves an import, not a run.

And here is that same service in the Google Cloud console. Cloud Run, us central one, healthy, scaling from zero. The request graph is live, and the logs underneath it are this recording's own traffic arriving at the deployed service, timestamped tonight. Firestore holds every clearance as an append only document. None of this is a screenshot of an intention.

This is the Agent Work Record Witness, and the gate is only its intake. The record is the product.

Before we pointed this at anyone else, we pointed it at a month of our own agent output. It said forty one point seven percent of commit claims disagreed with the repository. That number was wrong, and the error was ours. Seventy three of a hundred and three were real commits in a sibling repository on the same disk. The corrected figure is eight point one percent.

Here is a held claim, opened. It resolves all the way back to the session that produced it. That join is the thing no observability tool, no code reviewer and no compliance product holds, because none of them keeps the transcript.

Every Google service on the request path, with the probe that shows it. The queue humans actually open, calm by default. And the record, exportable, append only. Zero percent cleared. Nothing has passed, and that is the honest state, not a broken demo.

Installing it is a policy URL and one GitHub action. The probe runs inside your own checkout, where the repository already is. Only the verdict and a session pointer cross the network, so this product never needs read access to your code.

The policy is explicit. Enforce mode, scoped to pull requests an agent actually opened, with a named break-glass role for the human who overrides.

One real agent pull request went through the gate. Verify claims went red. It failed on purpose, and it is held.

What is next is the half we are asking for help with. Separating a citation from a claim. Committed as X, and commit X landed, are the same string. Only the speech act differs. And the thing we do not have yet: a single install by somebody who is not us.

The Agent Work Record Witness. Run your agents. Check the math.
