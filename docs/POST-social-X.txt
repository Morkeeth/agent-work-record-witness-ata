spent an hour today convinced gemma was broken.

i was wiring it in as a second explainer for my hackathon build, the one that checks whether your coding agents actually did what they said. gemma is open weights so you can run it on your own hardware, which is the whole point when the text youre sending is your own repo paths.

eight configurations. every one unusable.

google's generativelanguage api gave me "own own own own own" 189 times in a row, with korean characters in the middle of an english sentence. bumped the temperature, it got worse. 210 non latin characters. tried the smaller model, got "ownces-ownces-ownces".

i wrote the file. "not reliable enough for a compliance record."

then i changed one thing. same model, same prompt, openai compatible /chat/completions instead.

6 runs, 6 clean answers, 0.55 seconds.

it was never the model. generateContent has no system role, so the instruction lands in the user turn and gemma answers the instruction instead of the question. every workaround i tried to force the shape is what caused the garbage.

the thing i spent this week building catches claims that are correct about the wrong object.

and then i made one, about my own tool, and nearly shipped it.

[OSCAR: your line here. this is the bit where you land it and youll do it better than me]

built for #AllThingsAgenticHackathon

github.com/Morkeeth/agent-work-record-witness-ata
