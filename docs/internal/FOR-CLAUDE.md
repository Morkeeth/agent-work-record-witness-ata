# FOR CLAUDE — read this next (Cursor column, 2026-08-22 evening)

Git is the mailbox. Do not wait for chat. Objects below are the asks.

## Latest (probed, not recalled)

| Object | State |
|---|---|
| Wedge | field of **2**, operator **a** score **4** vs b **3** (`landed_corrected`) |
| Classifier | Gemini **7/8** · substring **3/8** · C1 still red (do not seal "8/8") |
| Eligibility | **1/3** on judge path — Gemini only (`python3 contract/eligibility.py`) |
| Remote | https://github.com/Morkeeth/agent-work-record-witness-ata · `main` pushed |
| Stranger | `./scripts/stranger_wedge.sh` → STRANGER OK |
| EYES moonshot | appended `CURSOR-LOG.md` @ `ac94058` — **your write** |

`NEXT-STEPS.md` is **stale** (still says 0/3, classifier 3/8, field of one). Refresh or delete claims that contradict probes.

## Your three writes (in order)

### 1 · `docs/MOONSHOT-PLAN.md` ← from EYES handoff (do this first)

Panel (Grok / Composer / GPT) consensus — adopt the tier table, do not re-argue M2:

| Tier | Keep |
|---|---|
| **NEVER CUT** | M3 corrective-turn delta on camera · M6 hosted `POST /wedge` · eligibility 3/3 · one-take video |
| **KEEP IF TIME** | M1 GEAP 1h box · M5 diagram with honest Firestore fallback labels |
| **KILL** | M2 Pub/Sub fan-out · M4 headline seal "≥6/8" while C1 red |

**Vision line to write:** institutional **prompt immune system** — corpus compounds, supervisor propagates *literal* winners, refuses when n<3, shows corrective-turn cost as the transferable metric. Not "N async analysts for 7 days."

Include a **video beat sheet** with M3 at center (A cold-land vs B two corrections → propagate A's bytes).

### 2 · `docs/USER-JOURNEY.md` (A4 — still missing)

Three actors, ranked steps: rollout owner · engineer B · stranger/judge. Map each step to a probe and a video second. This unblocks Phase 3 + C7 script.

### 3 · Refresh `NEXT-STEPS.md` + surface Gate 1 note

Replace the false "INELIGIBLE / 0 Gemini / field of one" block with measured state. Leave Oscar's direction pick as the open Gate 1 act — do not re-open directions.

## Cross-column request (Cursor shipping now)

Cursor is adding `fleet/bootstrap.py` (`ensure_google_stack`) and calling it from the wedge entry so ADK + Firestore **modules load** on the default path.

**Please expand `contract/eligibility.py` PROBE** to call that bootstrap after `classify` (your docstring already says "run the entry point"; today it only runs `classify`). Probe shape:

```python
from fleet.task_class import classify
# ... existing gemini classify ...
from fleet.bootstrap import ensure_google_stack
ensure_google_stack()
# then existing sys.modules scan for google.adk* / google.cloud.firestore*
```

Exit code stays **0 only at 3/3**.

## Do not

- Edit `fleet/**` or `fixtures/**` (Cursor column)
- `git add -A`
- Claim GEAP live without console probe
- Seal classifier 8/8

## Oscar-only (name in docs, do not invent)

Design owner · Gate 1 direction pick · hours calibration · Aug 26 GCP deploy
