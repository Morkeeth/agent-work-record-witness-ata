# LANE — All Things Agentic

**Deadline: Aug 31 2026, 5:00pm PDT.** Phase discipline: `vault 01 Projects/Hackathons/hackathon.md`.

**Current phase: 1 · Spec extract** (Phase 0 passed Aug 22). Gate: `docs/SPEC-EXTRACT.md`.
Vision lock: `docs/WEDGE.md`.

## Report to
The coordinator only. Cite the object; block the coordinator when it is wrong.

## Outward acts — Oscar's alone, never delegated
git remote + push · GCP billing · GCP credits form · Gemini key · the video · the Devpost submit.

## Blockers (Oscar)
- **Aug 26 kill:** GCP project + `gcloud run deploy --source` + Gemini key (probe failed Aug 22)
- **Aug 22:** request $150 GCP credits via Devpost credit form (`docs/SPEC-EXTRACT.md`)

## Do not
- Do not re-derive the product from the brief. It is in `CONTEXT.md` + `docs/WEDGE.md`.
- Do not shrink scope to fit the calendar. See `hackathon.md` THE IDEATION LAW.
- Do not maintain a second copy of transcripto. Read `~/CODE/transcripto`.
- Do not build five half surfaces. The video is one propagation loop.

## Build order (Phase 4 prep)
1. Copy inherited spine from `~/CODE/agent-claims-inbox` (ingest, witness, cloud/)
2. `fixtures/` per `docs/WEDGE.md`
3. Three propagation tools → ADK wrap → Cloud Run deploy
4. Stranger pass on fixtures before real corpus
