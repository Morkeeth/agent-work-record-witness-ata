> ⛔ **SUPERSEDED 2026-08-27 — do not read as current.** The canonical doc is `hack.md` at the repo root.
> This file is kept for history only. It described a narrower product than the one being built.

# MOONSHOT PLAN — institutional prompt immune system

**Adopted from EYES panel (Grok / Composer / GPT), 2026-08-22.** Binding for Week 0.

## Vision (one sentence)

> GEAP governs agents. Nothing governs prompts. The corpus compounds; the supervisor finds
> literal winners by corrective-turn cost, refuses when the field is thin, and writes the
> winner into the org skill file — witnessed, not rewritten.

Not "N async analysts for 7 days." The network is **Memory Bank of ranked prompts + Registry
of supervisors + propagate queue**.

## Tier table (do not re-argue)

| Tier | Keep |
|---|---|
| **NEVER CUT** | M3 corrective-turn delta on camera · M6 hosted `POST /wedge` · eligibility 3/3 · one-take video |
| **KEEP IF TIME** | M1 GEAP 1h box · M5 diagram with honest Firestore fallback labels |
| **KILL** | M2 Pub/Sub fan-out · M4 headline seal "≥6/8" while C1 red |

## Video beat sheet (≤4 min, M3 center)

| Sec | Beat | On screen |
|---|---|---|
| 0–20 | Problem | seats/spend vs practice; 95% non-human `user` turns |
| 20–50 | Authorship gate | show the trap; probe named |
| 50–110 | **M3 — A vs B** | `surface/org-proof.html`: A 0 corrective · B 2 · same task |
| 110–150 | Propagate + witness | literal bytes land; `VERIFIED-BY-REPO` |
| 150–190 | Eligibility / Cloud | `contract/eligibility.py` 3/3 · Cloud Run URL or Firestore console |
| 190–240 | Honest limit + ask | population lift = day-two org data; sealed prediction |

## Reproduce the M3 center now

```bash
python3 fleet_cli.py prove
open surface/org-proof.html
python3 contract/eligibility.py   # 3 OF 3 MET
```
