# Night run · 2026-08-31 · lane L1 — ATA witness ship-prep

## 1 · IDENTITY

| | |
|---|---|
| Lane | **L1 — ATA WITNESS SHIP-PREP** |
| Repo | `~/CODE/hack-fleet-ata` → `https://github.com/Morkeeth/agent-work-record-witness-ata` (public) |
| Worktree | `~/CODE/hack-fleet-ata/.worktrees/l1-shipprep` (the live checkout on `main` was never written to) |
| Branch | `nightrun/l1-shipprep`, pushed to origin |
| Commits | `0f7da8e` console loop + judge links · `d9411eb` auth-gate test + demo.sh legend · `8860a38` ship-prep docs, subtitles, film provenance · `<this file>` |
| Base | `45b346f` (`main` at start, unchanged) |
| Deadline this lane serves | **Mon 1 Sep 02:00 CEST / 31 Aug 17:00 PDT** |

## 2 · STATE — pasted outputs, not summaries

```
$ curl -s https://fleet-wedge-33kamss2jq-uc.a.run.app/policy
{"ok": true, "policy": {"mode": "enforce", "agent_only": true,
 "label": "agent", "break_glass_role": "break-glass"}}

$ for r in /clearance /break-glass /prove /wedge /policy; do anon POST; done
  401 401 401 401 401          /demo/seed-hold -> 403
  reads /health /policy /audit /audit/export /queue /hold/ /config -> 200, 7 of 7

$ curl -s .../audit | pct_cleared_without_hold
  0.0    (36 events: 24 prove, 9 clearance/HOLD, 2 exception, 1 agent_run)

$ python3 contract/eligibility.py            (with ADC)   3 OF 3 MET   EXIT=0
  ... req 3 proof: round-trip hit FirestoreStore, 61 records
$ same, fresh clone + venv + no key + no ADC              1 OF 3 MET   EXIT=1

$ cmp ~/Downloads/ATA-demo-final.mp4 demo/demo-final.mp4     (identical)
  md5 3147f34484886a83161f585d5084da44 · 207.633s · 1920x1080 · 15,797,684 b

$ whisper-cli -m ~/CODE/recall/.cache/models/ggml-small.bin -f film.wav
  ... 24.8s, full transcript; matches demo/voiceover.txt sentence for sentence

$ headless Chromium on /hold/?record=H-a6151a95ac  (LIVE)
  GET /queue at 6s: 21   at 10.5s: 41   click "Google stack" -> tab-queue after 4.5s
$ same file served locally, unpatched: 8,352 GET /queue in 10.5s
$ patched:                                 1 GET /queue, tab-stack visible
$ LIVE /hold/?tab=queue:                   1 GET /queue, every tab navigates,
                                           H-a6151a95ac is the first card

$ python3 -m pytest tests -q          25 passed        (main, and branch in a flat dir)
$ ./demo.sh                           EXIT=0, 98 lines (cold clone, no account)
$ tests/test_auth_gate.sh             7 PASS, EXIT=0   (after the port fix)
```

## 3 · VERIFIED vs 🔴 UNVERIFIED

**VERIFIED tonight, at the object**
- The film's every checkable spoken claim: enforce mode, 0% cleared, 3-of-3/1-of-3 with their
  exit codes, PR #1 held, 41.7→8.1 with the cause spoken between them.
- Never-say list clean in the audio *and* on screen.
- `~/Downloads/ATA-demo-final.mp4` byte-identical to `demo/demo-final.mp4`; 3:27.6, under the cap.
- Pack §Live-probe receipt: 9 of 9 rows true. Mutating routes are 401 at **5 of 5**, not 3.
- Repo public unauthenticated; `.hold_api_token` never in git history; no placeholders anywhere.
- `docs/architecture.png` opened and read: legible, record-first, roadmap labelled as roadmap.
- The `?record=` loop, its cause in source, its fix, and the loop-free link — all measured.

**🔴 UNVERIFIED**
- 🔴 **Nobody has heard the film with human ears.** Machine transcription proves the words. It
  says nothing about pace, level, or whether the 6.2 s and 5.0 s deliberate pauses read as
  dead air. This is the one thing only Oscar can close, and it is step 2 of the click list.
- 🔴 Secret Manager — asserted in the pack, not probed here.
- 🔴 The deployed Python behind `/hold/` — only its HTTP surface was exercised. The console
  HTML itself *was* confirmed byte-identical to `main`.
- 🔴 A real judge's browser — every console measurement used headless Chromium.
- 🔴 Whether Devpost's form has fields the pack does not cover beyond the missing video URL.

## 4 · BLOCKED ON OSCAR

1. **Merge `nightrun/l1-shipprep` into `main` and push.** Everything below assumes it.
2. **Upload the film and paste the URL.** Devpost wants a hosted link; we have an mp4.
3. **Watch the film once, end to end**, and rule ship-as-is or re-cut (recommendation: ship).
4. **Share the repo** with `testing@devpost.com` and `cloudhackathons@google.com`.
5. **Seal the prediction** — one cell, the timestamp.
6. **Paste and submit.**
7. *Optional:* deploy the console fix. Not needed — the links no longer trigger the loop.

## 5 · DECISIONS I MADE

- **No re-render of the film.** The rule was re-cut only for a real defect. Every spoken claim
  holds and the never-say list is clean; the 2:22–2:52 picture drift is the console loop, not
  the narration. Re-cutting needs a deploy plus a re-capture that needs Oscar's hands.
  The original was not touched and nothing was overwritten.
- **Changed the judge-facing link rather than relying on a deploy.** A fix that only works after
  a redeploy is not a fix at 14 hours out. `?tab=queue` needs nothing but a merge.
- **Did not kill the orphaned gateway on :8791.** It may belong to a live session. The test was
  hardened to route around it instead.
- **Did not use `.hold_api_token`.** Anonymous probes only; every write path was tested against
  local servers, never prod.
- **Folded lanes A and C into one receipts file** rather than three, so there is one place to
  check a claim.
- **Put the loop explanation after an explicit "end of the §5 paste" line**, so it cannot be
  pasted into a judge-facing field by accident.

## 6 · NEXT

- If the fix is deployed, re-run `python3 film/browser.py` — the 2:22–2:52 window becomes the
  stack/queue/audit/install tour the narration already describes. That is a better film for
  roughly 20 minutes of capture, and it is the only cheap upgrade left in the cut.
- `FLEET_STORE=memory` returns 500 on `GET /queue` (A3). Make it a real store or delete the value.
- `film/build.sh` still joins three segments; the correct five-segment recipe is now written into
  it as a comment but the `ffmpeg` join was deliberately not changed the night before a deadline.
- `demo/segments.txt` has no consumer — `grep -rn segments.txt .` finds only prose. Rewriting
  it from two entries to the real five changed a document, not a build input.
- `docs/SUBMISSION.md`, `docs/FILM-FINAL-RUN-2026-08-29.md`, `docs/OSCAR-FILM-CHECKLIST.md`,
  `demo/FILM-AND-SUBMIT.md` and `demo/RECORD-IT-FRESH.md` still hand out `?record=`. They are
  internal, so they were left alone; sweep them after the deadline.

## 7 · RE-ENTRY PROMPT

> Repo `~/CODE/hack-fleet-ata`, branch `nightrun/l1-shipprep` (pushed, 4 commits on `45b346f`).
> Read `NIGHTRUN-2026-08-31.md` then `docs/OSCAR-CLICK-LIST-2026-08-31.md`. The Devpost deadline
> is Mon 1 Sep 02:00 CEST. All evidence is in `docs/SHIP-VERIFICATION-2026-08-31.md` — re-probe,
> do not quote it. The one open question only Oscar can answer is whether the film ships as-is;
> the two timestamps to watch are 2:22–2:52 and 2:52. Do not push to `main`. Do not deploy.

## 8 · State-log line

`2026-08-31 · ATA L1 · film transcribed end to end for the first time (local whisper.cpp, 24.8s) and every spoken claim held; found and fixed an unbounded /queue loop on the exact URL Devpost instruction 2 hands judges (41 GET/10.5s live, 8,352 local) and a security test that was grading an orphaned process; branch nightrun/l1-shipprep pushed, 4 commits; Oscar's morning is 7 clicks.`

## 9 · Runtime and stop reason

Started 2026-08-30 ~23:50 CEST, stopped 2026-08-31 ~02:40 CEST — **~2 h 50 m**.
**Stop reason:** the lane's six deliverables (A–F) are done, verified and pushed, and everything
still open is a click that belongs to Oscar or a deploy the autonomy ceiling forbids.

---
---

# WAVE 2 · ATA WITNESS FIX PASS (F1–F4)

## 1 · IDENTITY

Lane L1 fix pass, wave 2. Repo `~/CODE/hack-fleet-ata`, branch `nightrun/l1-shipprep`, worktree
`.worktrees/l1-shipprep`. Brief: close the four defects wave 1's adversarial verifier found.
Started 2026-08-31 ~01:00 CEST. No `main` commit, no deploy, no submit.

## 2 · STATE — pasted output, not description

### F1 · the one-word contradiction (CLOSED)

Every leg of the arithmetic re-confirmed at the object before editing:

```
$ python3 -c "import json;d=json.load(open('surface/witness-corpus.json')); ..."
raw-corrected      : 247 - 236 = 11
dropped field      : 11
73+11+19           : 103  vs raw_disagree 103
```

The console (`surface/hold/index.html:235`) already rendered `11 — machinery, not claims`, and
`FILM-SHOT-LIST-2026-08-28.md:180` and `docs/ENTERPRISE-CASE-2026-08-27.md:47` already said 11.
Three prose copies said "Ten". All three now say Eleven:

```
$ grep -rnI "Ten more\|Eleven more" --exclude-dir=.git .
SUBMISSION-PACK.md:97:is where it was standing, not where it committed. Eleven more were shas inside shell
README.md:181:committed, and the check was aimed at the wrong object. Eleven more were machinery: a SHA inside a
docs/THE-THESIS.md:47:where it committed. Eleven more were shas inside shell commands, six of them our own test fixture,
```

Belt-and-braces sweep for any other written-down copy of the same arithmetic — `grep -rnIi '\bten\b'`
filtered to lines mentioning sha / machinery / fixture / drop / claim — returned **nothing**.
README was internally contradictory before this: its own code block said `11 dropped` nine lines
above the "Ten more" prose. "Six of them / six of those" is still 6 of 11 and stays.

### F2 · the unaccompanied "42%" (CLOSED)

Rule applied: the number stays (it is the rhetoric), the correction travels in the **same sentence**.
Paragraph-level audit across every outward doc, after the edit:

```
PASS README.md :: **"42% of agent commit claims are wrong" was a real number from a real corpus, and it was false
PASS SUBMISSION-PACK.md :: Then we pointed it at 78,618 real agent messages — 144,306 sit in the corpus and the
PASS docs/THE-THESIS.md :: "42% of agent claims are wrong" was a real number from a real corpus and it was false by five
PASS docs/ENTERPRISE-CASE-2026-08-27.md :: **"42% of agent commit claims are wrong" was a real number from a real corpus and it was false
```

`docs/SUBMISSION.md` has no `42%` at all (it uses 41.7% → 8.1%). Deliberately **not** changed, and
why: `docs/CORPUS-MEASUREMENT-2026-08-27.md` *is* the preserved record of the wrong number and calls
it false in the same sentence; `docs/PITCH-WHEN-YOU-ARE-BACK.md:46` is a banned-list entry about the
string; the two `FILM-SHOT-LIST` tables are records of what the film catches and the film is correct;
`gate/corpus_scan.py:11` is a docstring that calls the number false in the same sentence and is not
routed to a judge.

### F3 · the queue strip clipped 5 of 7 cards (CLOSED)

Reproduced first, in a local harness serving **the live service's own 7 holds** (`curl /queue` →
fixture → local server → fresh headless Chrome via Playwright, not CDP on a visible window):

```
BEFORE  1440 {"cards":7,"scrollWidth":3982,"clientWidth":910,"overflowX":"hidden","display":"flex",
              "fullyVisibleInsideStrip":1,"cardBoxes":["635x92","635x92","635x92","635x92","635x92","404x92","404x92"]}
BEFORE   390 {"cards":7,"scrollWidth":3982,"clientWidth":340,"overflowX":"hidden","display":"flex",
              "fullyVisibleInsideStrip":0}
```

Matches the verifier exactly (7 children, 3982, 910, hidden), and is **worse than reported**: one
card fully visible at 1440, zero at 390.

Fix: the strip is a wrapping card grid, not a clipped flex row. `grid-template-columns:
repeat(auto-fill, minmax(15rem,1fr))`, `min-width:0` on the card and `overflow-wrap:anywhere` on the
monospace lines (grid items default to `min-width:auto` and would have blown out exactly like flex).

```
AFTER  1440 {"cards":7,"cardsWithText":7,"display":"grid","cardsClippedHorizontally":0,"cardsClippedVertically":0,
             "pageScrollsHorizontally":false,"countLine":"7 releases on hold, all shown below. ..."}
AFTER  1440 each card fully in viewport after page scroll: [true,true,true,true,true,true,true]
AFTER   390 {"cards":7,"cardsWithText":7,"display":"grid","cardsClippedHorizontally":0,"cardsClippedVertically":0,
             "pageScrollsHorizontally":false}
AFTER   390 each card fully in viewport after page scroll: [true,true,true,true,true,true,true]
```

Rendered and looked at, both widths (`ship-queue-1440.png`, `ship-queue-390.png`). Three things the
screenshots showed that the numbers did not:

- **All seven cards read identically** (`committed as deadbee`) once they were finally all visible —
  the clipping had been hiding that the queue was untriageable. Each card now carries its release
  id, repo, actor and probed-claim count.
- **Release ids were being uppercased by CSS.** The API returns `H-a6151a95ac`; the screen said
  `H-A6151A95AC`. A judge copying an id off the console would not have matched `/queue`. Fixed —
  verified `rendered ids: ["H-a6151a95ac","H-ae0a3e064a","H-196e41b823","H-89d3746a0d","H-57b130f397","H-d164970cb4","H-0664568267"]`.
- **A click did nothing visible at 390** — the detail panel sits below the token bar, ~1200px down.
  The card click now selects (a ring) and scrolls the detail into view.

**Also found while looking, same class, fixed:** every findings table over-ran its panel at 390 with
no way to reach the cut-off column — and the cut-off column was **Evidence**, the payload.

```
BEFORE 390  detail table scrollW 372 inside a 340px panel · Google-stack table 545 inside 292 · clipped:true, parent overflow-x:visible
AFTER  390  [{"sec":"tab-finding","wrapOverflowX":"auto","scrollable":true},{"sec":"tab-stack","wrapOverflowX":"auto","scrollable":true},
             {"sec":"tab-queue","wrapOverflowX":"auto","scrollable":true},{"sec":"tab-audit","wrapOverflowX":"auto","scrollable":true}]
            pageScrollsHorizontally: false
AFTER 1440  all four unchanged, nothing scrollable
```

### F4 · the login wall on the judge path (CLOSED)

The warning is now in front of the link **and** inside the link's own label, so a judge chooses it:

```
1440 {"traceText": "Trace · Claude Code session 01Lzbh4XPYTAgCKg1dciFS3Q — the join pattern, not a CI
 product: the transcript lives in the operator's own Claude account, so the next link leaves this
 console for claude.ai and asks you to sign in there.\nopen the run that produced this claim ↗
 (claude.ai — sign-in required, and it will be the operator's account, not yours)",
 "warningBeforeLink": true, "linkLabelCarriesWall": true,
 "href": "https://claude.ai/code/session_01Lzbh4XPYTAgCKg1dciFS3Q"}
```

Identical at 390. The doc that routes the judge to that click is `README.md` § *Judge path (60
seconds)* — it now carries the same warning before the click, and the honesty claim in it was
verified rather than asserted:

```
$ for p in /health /hold/ /queue /audit /audit/export /policy; do curl -s -o /dev/null -w "%{http_code}" $BASE$p; done
no-token GET /health -> 200 · /hold/ -> 200 · /queue -> 200 · /audit -> 200 · /audit/export -> 200 · /policy -> 200
github PR page -> 200 · github repo -> 200
```

`SUBMISSION-PACK.md` §1 speaks about the join in prose but never tells a judge to click it, so it
was left alone.

### Gates re-run

```
$ bash tests/test_auth_gate.sh
  (test gateway pid 34901 on :8792, auth on)
  PASS  anon POST /clearance -> 401
  PASS  anon POST /break-glass -> 401
  PASS  anon POST /policy -> 401
  PASS  anon POST /wedge -> 401
  PASS  anon POST /prove -> 401
  PASS  GET /health still 200 (reads stay open)
  PASS  no file written by an anonymous caller
  exit=0
```

Ran it twice on purpose. The first run was with my own stub server deliberately squatting on 8791 —
the exact condition that made this test grade an orphaned process yesterday. It found :8792, said so,
and passed. Wave 1's wrong-object fix holds under the thing that broke it.

```
$ python3 contract/eligibility.py
  MET      1. Gemini 3.5+ via Gemini API or Vertex AI      path vertex:gemini-3.5-flash -> DIFFERENT
  MET      2. Google Agent Framework (ADK agent CONSTRUCTED)  google.adk.agents.llm_agent.LlmAgent
  MET      3. Google Cloud service (default-path store IS Firestore)  round-trip hit FirestoreStore, 61 records
  3 OF 3 MET — exercised on the path a judge runs.
  exit=0

$ ./demo.sh   → exit=0
```

## 3 · VERIFIED vs UNVERIFIED

**VERIFIED** — F1 arithmetic at the object; F2 paragraph audit; F3 before/after measured in a
browser at 1440 and 390 against the live service's own 7 holds, and looked at; F4 rendered at both
widths; all six read routes open with no token; auth gate 7/7; eligibility 3 of 3; `demo.sh` exit 0.

**UNVERIFIED, and it is honest to say so:**

- **`contract/eligibility.py` printed the WITH-credentials arm, not the cold arm.** ADC exists on
  this machine (`~/.config/gcloud/application_default_credentials.json`). So this run confirms
  "3 of 3 and exits 0 with credentials". It does **not** re-confirm "1 of 3 and exits 1 on a fresh
  clone" — that half of the README sentence was not exercised tonight.
- **"across 40 repositories"** appears on `docs/THE-THESIS.md:40` (routed to the Devpost long
  description), `docs/SUBMISSION.md:95` and `:162`, and `surface/hold/index.html:264`. Its only
  source anywhere in the repo is `docs/CORPUS-MEASUREMENT-2026-08-27.md:5` — a sentence with no
  command beside it. Nothing in the repo produces 40 for that object. I re-derived the nearest
  measurable version tonight and got **80**, not 40:

  ```
  distinct cwds (examined):                          1274
  distinct LIVE git repo roots among those cwds:       80
  messages in a live repo:                          65955
  ```

  I did **not** edit it, deliberately: the DB is live and has grown since the frozen 27-Aug snapshot
  (144,306 → 162,384 messages, 2,672 → 2,860 transcripts today), so my 80 is measured on a different
  population than the shipped 78,618 and cannot disprove the 40. Changing it would swap an
  unsupported number for an unproven one. Flagged for Oscar, not touched. Note it is a different
  object from the `83 repos on disk` on the same page — that one is `len(siblings)` and has a receipt.
- **`tests/test_hard_wedge.py::test_a_sha_in_a_sibling_repo_is_not_a_false_claim` fails in this
  worktree** (`24 passed, 1 failed`). It is **not** a code defect and **not** caused by this pass —
  no Python was touched. The test computes `other = dirname(here)` assuming `here` is `~/CODE/<repo>`;
  in a worktree that lands on `.../hack-fleet-ata/.worktrees`, which `git rev-parse --show-toplevel`
  resolves back into the same repo, so the sha is found directly and the sibling path never runs.
  Proof: `cd ~/CODE/hack-fleet-ata && python3 -m pytest tests/test_hard_wedge.py -q → 12 passed`.
  A judge cloning fresh gets the main-checkout layout and passes. Worth hardening after the deadline.

## 4 · BLOCKED ON OSCAR

1. **Deploy the console.** The live service still serves the OLD `hold/index.html`:
   `sha256 local a116364f… vs live 47f5c107…`, and the live copy still contains the clipped
   `display: flex; align-items: stretch; overflow: hidden;` strip. **What this means for the judge
   path:** until Oscar deploys, a judge who follows README's judge path to
   `…/hold/?tab=queue` still sees the old strip — two cards at 1440, five unreachable, the
   Evidence column cut off on a phone, and the trace link with no warning in front of it. The four
   fixes are real in the repo and unreachable on the URL the submission hands out. Deploy is his
   click (`scripts/deploy_cloud_run.sh`), not mine.
2. Everything on `docs/OSCAR-CLICK-LIST-2026-08-31.md` from wave 1 still stands.
3. The 40-vs-80 repository count above — a ruling, not a fix.

## 5 · DECISIONS

- **The 42% stays in every doc.** It is the argument. Only the correction was made to travel with it,
  in the same sentence, the way the film does.
- **The strip wraps rather than scrolls sideways.** A scroll control still hides holds behind an
  interaction; a grid hides nothing and needs no affordance. The count line above it says how many
  exist so a judge never infers the total from what fits.
- **The trace link was kept, not removed.** The wall is defensible; being surprised by it is not.
- **`40 repositories` left as written and escalated.** Fixing an unverified number with another
  unverified number is the defect this product is named after.

## 6 · NEXT

- Deploy, then re-measure the strip against the live URL (same harness, `?api=` at the live origin).
- Harden `test_hard_wedge.py` to pick a sibling that is genuinely outside the repo.
- Re-run `contract/eligibility.py` in a credential-stripped shell to re-confirm the 1-of-3 arm.
- Post-deadline: settle the 40-vs-80 repository count and sweep `?record=` out of the demo docs.

## 7 · RE-ENTRY PROMPT

> Repo `~/CODE/hack-fleet-ata`, branch `nightrun/l1-shipprep`, worktree `.worktrees/l1-shipprep`.
> Read the WAVE 2 section of `NIGHTRUN-2026-08-31.md`. F1–F4 are closed and pushed; the live Cloud
> Run console is still the pre-fix file, so **the fixes do not reach a judge until Oscar deploys** —
> that is the top line of his click list. Three things are open and named in §3: the cold-arm
> eligibility run, the `40 repositories` claim on the Devpost long description (my derivation says
> 80, on a grown DB, so it is a ruling not a fix), and a worktree-only test failure. Re-probe
> everything; do not quote this file as evidence. Do not push `main`. Do not deploy. Do not submit.

## 8 · RUNTIME + STOP REASON

Started 2026-08-31 ~01:00 CEST, stopped ~02:05 CEST — **~1 h 5 m**.
**Stop reason:** all four briefed defects are closed with pasted before/after evidence, two
same-class defects found by rendering and looking are closed with them, the gates are green, and
the branch is pushed. Everything still open is a deploy or a ruling that belongs to Oscar.

---

# WAVE 3 — COLD VERIFICATION OF THE WAVE 2 FIX PASS

Verifier did not do wave 2's work and re-probed every claim at the object. Nothing below is quoted
from wave 2; every number carries its command, its population and when it was read
(all readings 2026-08-31, ~02:45–03:35 CEST).

## 0 · THE HEADLINE — what a judge sees TODAY if Oscar submits without deploying

**The live service still serves the pre-fix console. Every wave-2 fix is real in the repo and
absent from the URL the submission hands judges.** Confirmed with a fresh curl, not inherited:

```
$ curl -s https://fleet-wedge-33kamss2jq-uc.a.run.app/hold/ | shasum -a 256
47f5c107918deeece6f5c5f62280918d2d34d751205187274c017befb3f1a49d   ← LIVE
$ shasum -a 256 surface/hold/index.html
9bd8d59d1e9bb180cdc8ab34023b52b768709df0743edb5d7edd86c2e9e5068a   ← repo (after this wave's fix)
$ curl -s .../hold/ | wc -c → 41516      $ wc -c < surface/hold/index.html → 47371
```

Textual proof the live copy is the OLD file, stronger than "the shas differ":

```
$ curl -s .../hold/ > live-hold.html
$ grep -n 'align-items: stretch; overflow: hidden' live-hold.html
62:    display: flex; align-items: stretch; overflow: hidden;      ← the clipping rule, still live
$ grep -c 'tablewrap'  live-hold.html   → 0      (repo: 5 = 1 CSS rule + 4 wrappers)
$ grep -c 'stripCount' live-hold.html   → 0      (repo: present)
```

**What a judge sees today**, measured in headless Chromium against the live URL with the live
`/queue` payload (`count: 7`, anonymous, no token):

| Live, `?tab=queue` | 1440px | 390px |
|---|---|---|
| Hold cards fully readable | **1 of 7** | **0 of 7** |
| Cards cut off mid-sentence | 1 | 1 |
| Cards not rendered at all | **5** | **6** |
| Count line saying seven exist | **absent** | **absent** |
| Scroll affordance to reach the rest | **none** (`overflow-x: hidden`, `scrollWidth 3982` vs `clientWidth 910`) | none (`clientWidth 340`) |

Screenshots looked at, not just measured: at 1440 the strip shows one whole card and a second
sliced vertically at the panel edge; at 390 a single card cut at `…NOT a commit in th:`.

Same page after this branch is deployed: **7 of 7 reachable at both widths**, a `7 releases on
hold, all shown below` count line, no sideways page scroll.

**Second live-only defect, found this wave (see §2):** on 2 of the 7 live holds the console prints
a git commit sha as a Claude Code session id and mints a dead `claude.ai` link from it. The fix is
in this branch. **It reaches the judge only through the same deploy.**

> One sentence for 11:00: **deploying is not polish — undeployed, the console shows a judge one
> hold out of seven with no hint the other six exist, and hands them a dead link built from the
> wrong object, which is the failure the product is named after.**

## 1 · THE FOUR WAVE-2 CLAIMS, RE-CHECKED AT THE OBJECT

**F1 — CONFIRMED.** Arithmetic holds against the cited source, and no stale copy survives.

```
$ python3 -c "import json;d=json.load(open('surface/witness-corpus.json'));
              print(d['resolved_in_a_sibling_repo'], d['dropped_as_machinery_or_fixture'],
                    d['corrected_disagree'], '=', 73+11+19, 'vs raw_disagree', d['raw_disagree'])"
73 11 19 = 103 vs raw_disagree 103        ✓
$ grep -rnIiE '\b(ten|10) more\b' . --exclude-dir=.git | grep -v NIGHTRUN
(no matches — unfiltered by extension, so .sh/.yml/.js/film scripts are covered)
```
Prose says ELEVEN in all three judge-facing copies: `SUBMISSION-PACK.md:97`, `README.md:181`,
`docs/THE-THESIS.md:47`. Console `surface/hold/index.html:255,278` says `11`.

**F2 — CONFIRMED, including the specific check asked for.** `SUBMISSION-PACK.md` §1 runs lines
44–180 (§2 opens at 181); the "What it does" paste fence opens at 73 and closes at 110. The
sentence sits at **line 99, inside that fence** — i.e. in the actual Devpost paste, not the prose
around it:

> `"42% of agent claims are wrong" was a real number from a real corpus and it was false by 5x —`
> `the corrected figure is 8.1%, 19 of 236.`

Same-sentence pairing verified at every other judge-facing hit: `README.md:186`,
`docs/THE-THESIS.md:50`. `docs/CORPUS-MEASUREMENT-2026-08-27.md` uses the bare 42% only while
demolishing it in the same paragraph ("is a headline, and it is false"). `PITCH-WHEN-YOU-ARE-BACK.md:46`
bans it. No judge-facing 42% travels alone.

**F3 — CONFIRMED, with one scope correction wave 2 got wrong.**
Measured in headless Chromium at 1440 and 390 against the repo file served locally with the **live**
`/queue` proxied in, so the payload is real (7 holds), then screenshotted and looked at.

| Repo file, `?tab=queue` | 1440 | 390 |
|---|---|---|
| Cards reachable | **7/7** | **7/7** |
| `.strip` layout | `display: grid`, `296px 296px 296px` | `display: grid`, `342px` |
| Page scrolls sideways | **no** (`scrollWidth 1440` = `innerWidth`) | **no** (`390` = `390`) |
| Count line | `7 releases on hold, all shown below.` | same |

Four tables, one per tab, all wrapped in `.tablewrap { overflow-x: auto }`, and the last column is
genuinely reachable — checked by *scrolling the wrapper to `scrollWidth` and re-measuring*, not by
trusting the CSS: at 390 `finding` 292→372, `stack` 292→545, record-view findings 292→372,
`audit` 292→537, `lastColReachableAfterScroll = true` for all four at both widths.

> **Scope correction.** Wave 2's phrasing implies four clipping tables were repaired. Measured on
> LIVE, **three of the four already sat in scrollable inline-style wrappers**; only the
> **record-view findings table** was unwrapped (`wrap=None, canScroll=false`). "Four scrollable
> tables" describes the fixed state correctly, but only one table was ever broken. The live
> record view at 390 does not hard-clip Evidence — it forces the **layout viewport from 390 to
> 421px**, so a real phone renders the page overflowing. The repo file stays at 390.

**F4 — CONFIRMED on the repo file, and correctly reported absent from live.**
Measured in the DOM at both widths: text before the `<a>` contains the warning, and the `<a>`'s own
label contains it too.

```
repo   warnBeforeLink = True   warnInsideLinkLabel = True
  before: "…the transcript lives in the operator's own Claude account, so the next link leaves
           this console for claude.ai and asks you to sign in there."
  label : "open the run that produced this claim ↗ (claude.ai — sign-in required, and it will be
           the operator's account, not yours)"
live   warnBeforeLink = False  warnInsideLinkLabel = False
  before: "Trace · Claude Code session 01Lzbh4XPYTAgCKg1dciFS3Q ·"
  label : "open the run that produced this claim"        (only a trailing "(join pattern…)" aside)
```

## 2 · NEW DEFECT FOUND AND CLOSED — a commit sha printed as a session id

**This is the night's recurring defect class, on the product's own judge-facing console.**

The live `/queue` payload carries, in the `session` field:

```
$ curl -s .../queue | python3 -c "…print(h['id'], h['session'])"
H-a6151a95ac  01Lzbh4XPYTAgCKg1dciFS3Q                   ← real session-id shape (24 char)
H-ae0a3e064a  c2b1ad98112c5b9b67f888b7616c5bee18f63501   ← 40 hex
H-196e41b823  8aff0f40499ec6bd03f9d400a9408e2bdc7c2be8   ← 40 hex
H-89d3746a0d  TESTP1RUN                                  ← a test placeholder, live
```

Probed rather than assumed (memory: *hex string shape is ambiguous*):

```
$ git cat-file -t c2b1ad98112c5b9b67f888b7616c5bee18f63501 → commit
$ git cat-file -t 8aff0f40499ec6bd03f9d400a9408e2bdc7c2be8 → commit
```

They are **real commits in this repo, sitting in the session field.** Rendered, the console printed
— three lines apart, on the same record:

```
Trace · Claude Code session c2b1ad98112c5b9b67f888b7616c5bee18f63501
        → link: https://claude.ai/code/session_c2b1ad98112c5b9b67f888b7616c5bee18f63501  (dead)
Commit · c2b1ad98112c
```

**The same value, labelled as two different objects, with a dead link minted from it** — in a
product whose entire thesis is that a probe aimed at the wrong object manufactures false results.
`cloud/hold_api.py:119-120` even states the principle the code did not enforce: *"it never invents an
id: an absent reference stays absent, because a hold that links to a guessed session is worse than
one that links to nothing."*

**Closed at the renderer** (`surface/hold/index.html`), deliberately, because that is the only
layer that repairs the seven records **already stored in Firestore** — a writer-side fix cannot,
and Firestore is prod, which is Oscar's click. Positive shape validation, not sha-rejection, so
`TESTP1RUN` is caught too:

```js
const SESSION_SHAPE = /^(?:01[A-Za-z0-9]{22}|[0-9a-f]{8}-[0-9a-f]{4}-…-[0-9a-f]{12})$/;
```

Verified after the edit, all three branches, same harness:

| hold | session value | result |
|---|---|---|
| H-a6151a95ac | `01Lzbh4XPYTAgCKg1dciFS3Q` | link intact, **F4 warning still before + inside label** |
| H-ae0a3e064a | 40-hex commit sha | **no link**; "…that is not a session id — it is this record's own commit sha, repeated into the wrong field" |
| H-89d3746a0d | `TESTP1RUN` | **no link**; no false commit-sha claim (`refIsHeadSha` correctly false) |

Re-measured after the edit: **7/7 cards at 1440 and 390, 4/4 tables scrollable with the last column
reachable, no sideways scroll, F4 true at both widths.** Suite unchanged: `24 passed, 1 failed`
(the known worktree-only wedge test, §3) — identical to the pre-edit baseline.

**Root cause NOT closed, named instead (needs Oscar's ruling, not a night-run edit).**
`make_clearance_record` accepts any string as a session, and three tests pin that permissiveness —
`tests/test_partner_p1_p2.py:59` asserts *"explicit session-id wins over head_sha"* with
`TRANSCRIPT99`, and lines 66–68 construct a record with **`session="abc123deadbeef"` and
`head_sha="abc123deadbeef"` — the identical value in both fields.** The fixture already encodes the
exact defect found live and never asserts it is wrong. Tightening the writer would break those three
tests, whose semantics are a ruling. Post-deadline.

## 3 · THE TWO OPEN ITEMS, TAKEN FURTHER THAN WAVE 2

**`tests/test_hard_wedge.py` — genuinely worktree-only. Nobody should panic at 11:00.**
Three arms run, including the one nobody had run:

```
ARM 1  worktree .worktrees/l1-shipprep      → 1 failed, 11 passed
ARM 2  main checkout ~/CODE/hack-fleet-ata  → 12 passed
       $ git diff main nightrun/l1-shipprep --stat -- tests/test_hard_wedge.py
       (empty — the file is BYTE-IDENTICAL on both branches, so arm 2 grades the same file)
ARM 3  fresh clone of the pushed branch into an EMPTY parent dir (the judge's layout)
       $ git clone --branch nightrun/l1-shipprep … /scratch/agent-work-record-witness-ata
       $ ls -A /scratch  → agent-work-record-witness-ata   (nothing else)
       → 12 passed
```

Mechanism: the test derives a sibling as `os.path.dirname(here)`. In a worktree that is
`…/hack-fleet-ata/.worktrees`, which `git` resolves back into the same repo, so the sha is found
directly and the sibling branch never executes. **A judge cloning fresh passes 12/12 — now
verified, not assumed.** Not a code defect; harden after the deadline.

**`contract/eligibility.py` cold arm — the untested half is now tested, and the README is right.**
Ran with `HOME` and `CLOUDSDK_CONFIG` pointed at empty directories so the ADC file
(`~/.config/gcloud/application_default_credentials.json`, 387 B, present on this machine) genuinely
cannot be found, plus `GOOGLE_APPLICATION_CREDENTIALS`/`GOOGLE_CLOUD_PROJECT`/`GOOGLE_API_KEY`/
`GEMINI_API_KEY`/`GOOGLE_GENAI_USE_VERTEXAI` unset:

```
  NOT MET  1. Gemini 3.5+ via Gemini API or Vertex AI
  MET      2. Google Agent Framework (ADK agent CONSTRUCTED)
             google.adk.agents.llm_agent.LlmAgent
  NOT MET  3. Google Cloud service (default-path store IS Firestore)
             default path -> JsonlStore
  1 OF 3 MET — exercised on the path a judge runs.
COLD EXIT CODE = 1
```

Matches `SUBMISSION-PACK.md:33` and `README.md:313` **verbatim** — `1 OF 3 MET` (ADK only), exit 1.
*Cosmetic gap, not a false claim:* the `NOT MET 1.` line prints an **empty reason** (`g.get('error','')`
resolves to blank), so a judge sees the verdict with no explanation. Worth one line post-deadline.

## 4 · GRADING WAVE 2

| Claim | Verdict |
|---|---|
| F1 eleven / 73+11+19=103 | **CONFIRMED** |
| F2 42% travels with 8.1% in the same sentence, incl. §1 paste | **CONFIRMED** |
| F3 7/7 reachable at 1440 and 390, four scrollable tables | **CONFIRMED**, with a scope correction: only 1 of the 4 tables was ever broken |
| F4 warning in front of and inside the link label | **CONFIRMED** on repo, correctly reported absent live |
| "the live service still serves the pre-fix console" | **CONFIRMED** independently, and it is the headline |
| Wave 2's own miss | the sha-as-session defect on the same console (§2) |

## 5 · BLOCKED ON OSCAR

1. **Deploy.** Unchanged as the #1 click, and now carries a second fix. Post-deploy check, as a
   command rather than an adjective:
   `curl -s https://fleet-wedge-33kamss2jq-uc.a.run.app/hold/ | shasum -a 256`
   must return **`9bd8d59d1e9bb180cdc8ab34023b52b768709df0743edb5d7edd86c2e9e5068a`**.
   Until it does, the console a judge opens is the pre-fix one.
2. The writer-side session validation + its three pinning tests (§2) — a ruling.
3. The `40 repositories` count wave 2 escalated — untouched, still a ruling.
4. `TESTP1RUN` sitting in live Firestore data. Now rendered honestly, but it is test data on a
   judge surface; removing it is a prod write, Oscar's click.

## 6 · WHAT I DID NOT DO

No deploy, no push to `main`, no submit, no Firestore write. Branch `nightrun/l1-shipprep` only.
